#include <stdio.h>

int main(void) {
    int t,a,b;
	scanf("%d",&t);
	while(t--)
	{
	    scanf("%d%d",&a,&b);
	    if(a>=b)
	    printf("%d\n",a/(b*2));
	    else
	    printf("%d\n",0);
	}
	return 0;
}

