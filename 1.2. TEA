#include <stdio.h>

int main(void) {
	int t,a,b,c;
	scanf("%d\n",&t);
	while(t--)
	{
	    scanf("%d %d %d",&a,&b,&c);
	    if(a<=b)
	    printf("%d \n",c);
	    else if(a%b==0)
	    printf("%d \n",((a/b)*c));
	    else
	    printf("%d \n",((a/b)+1)*c);
	}
	    return 0;
}

